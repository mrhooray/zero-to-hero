pre-commit:
  parallel: true
  commands:
    nn:
      root: "nn"
      glob: "**/*.py"
      run: uv run ruff check --fix {staged_files} && uv run ruff format {staged_files} && uv run pytest
      stage_fixed: true
